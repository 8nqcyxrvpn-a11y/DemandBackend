services:
  - type: web
    name: ai-fashion-intelligence
    runtime: python
    buildCommand: python -m pip install --only-binary=:all: -r requirements.txt && python scripts/validate_artifacts.py
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
